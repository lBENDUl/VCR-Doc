#Enumeración #Linux 

Es un escáner que funciona a través de los paquetes ack y detecta los host que están en una red.

Para realizar un escaneo en la red local:

```
arp-scan -I ens33 --localnet --ignoredups
```

